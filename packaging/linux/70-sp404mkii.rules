# Roland SP-404MKII: let the logged-in user open its USB serial port, so SparkyMK2 works
# without joining the uucp or dialout group. The number keeps it before 73-seat-late.rules.
SUBSYSTEM=="tty", ATTRS{idVendor}=="0582", ATTRS{idProduct}=="02e7", TAG+="uaccess"
